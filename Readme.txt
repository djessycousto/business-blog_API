I will start with ejs, but first the backend


// control

I might have some error in JWT, and authenticateUser
// error composition 
 throw new CustomError.UnauthenticatedError("Authentication Invalid");
